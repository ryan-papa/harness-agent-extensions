# Changelog

## [0.29.0] - 2026-08-10

### Added
- rp-deck **타임라인 구간 컴포넌트 `.timeline` 신설** (`template.html`, `visual-patterns.md`, `design-rules.md`, `SKILL.md`) — DB 타임아웃 장표에서 구간 겹침을 그리려다 매번 인라인 SVG를 손으로 짜고 있었다. 재사용 가능한 컴포넌트로 승격한다
  - **`.bars`로는 안 되는 것을 그린다** — 막대는 길이만 비교하지만, 타임라인은 `x=시작 시각` / `width=길이`로 **어디서 시작해 어디까지인지**를 같은 축에 겹쳐 **포함·초과 관계**가 눈에 보이게 한다. 타임아웃·SLA·재시도·배포 윈도우·단계별 소요가 대상
  - **의미 색 6종 고정** — `outer`(바깥 예산)·`wait`(대기)·기본 파랑(핵심)·`ghost`(묶는 구간, 테두리)·`ok`(실측)·`bad`(초과). 밝은 막대 위 값은 `.tl-val.on-light`·`.on-ghost`로 대비를 지킨다
  - **축·눈금 필수** — 눈금 없이 막대만 있으면 길이가 근거 없는 그림이 된다. 시작점이 갈리는 지점은 `.tl-mark` 점선 + 한 줄 라벨
  - 샘플 슬라이드 1장 추가(22장) — DB 타임아웃 5구간을 `0초=x280`, `1초=80px` 한 축에 겹친 예시
- rp-deck **선택 규칙에 편입** — `visual-patterns.md` §2 매핑표·§3 사용 규칙·§7 높이 예산(행 n개 ≈ 36n+56), `design-rules.md` §3에 "시간·구간 값이 **3개 이상** 나열되면 `.timeline`" 한 줄. 규칙에 없으면 리뷰어가 못 잡고 생성자도 안 쓴다

## [0.28.0] - 2026-08-08

### Added
- **`rp-youtube-channel` 신설** — 채널의 공개 영상을 최신순으로 훑어 **장표로 만들 수 있는 후보만** 추린다. "하루 한 편씩 자동으로 장표를 올린다"는 파이프라인의 첫 단계
  - **상태를 장표 레포에 둔다** — 로컬 캐시(`~/.rp-deck/repos/...`)는 매 실행 `reset --hard`로 날아가고 기기를 바꾸면 사라진다. `<root>/.rp-youtube/`에 두고 push까지 완료해야 다음 실행이 같은 영상을 다시 고르지 않는다
  - **"이미 만든 것"은 상태 파일이 아니라 장표에서 읽는다** — 각 장표의 `<meta name="deck-source-video">`를 스캔한다. 상태 파일이 유실돼도 게시된 장표에서 복구되고, 완료 목록을 두 곳에 적어 어긋나는 일이 없다. 상태 파일은 **장표를 만들지 못한 이유**(멤버십·자막없음)만 담는다 — 이건 장표가 없어 기록할 데가 없다
  - **순차 프로브** — 목록 조회(`--flat-playlist`)는 ID·제목만 주고 공개일·길이·자막·`availability`는 건당 1~2초의 개별 조회가 필요하다. 수백 개를 전부 프로브하면 몇 분씩 걸리므로, 처리·제외된 것을 건너뛰고 남은 것만 앞에서부터 프로브해 `--limit`을 채우면 멈춘다 (실측: 180개 채널에서 후보 3건 확보에 프로브 3건·14초)
  - **제외는 영구, 보류는 재시도 가능** — 멤버십·Premium·비공개·90초 이하는 영구 제외(다시 프로브하지 않음), 자막 없음·조회 실패는 보류로 두고 `--retry-deferred`로 되살린다
  - **모음편 제외는 채널별 설정** — 기존 영상을 이어 붙인 편집본은 개별 영상과 내용이 겹친다. 표기가 채널마다 다르므로 정규식을 `registry.json`에 채널별로 둔다
  - **대상 지정 4종** — 핸들(`@name`)·채널 URL·**영상 URL(업로더로 역산)**·등록된 별칭. 인자가 없으면 등록 목록을 보여주고 물어본다
  - ⛔ 특정 채널의 핸들·URL·이름을 스킬 파일과 스크립트에 하드코딩하지 않는다. 전부 인자와 `registry.json`으로 받는다
  - **장표 메타는 영상 ID가 아니라 솔트 해시** — `deck-source-ref` = `sha256("<솔트>:<영상ID>")[:16]`. 장표 HTML은 공개 사이트로 나가므로 ID를 그대로 넣으면 rp-youtube-deck의 §영상 흔적 지우기가 무의미해진다. 솔트가 없으면 채널을 짐작한 사람이 그 채널 영상 ID를 전부 해시해 맞춰볼 수 있어, 솔트를 `<root>/.rp-youtube/salt`(레포 안, 공개되지 않음)에 둔다. `--ref <영상ID>`로 계산한다
  - **일시적 조회 실패를 상태로 굳히지 않는다** — 실측에서 레이트리밋으로 41건이 "조회 실패" 보류로 기록돼, 몇 분 전까지 멀쩡히 조회되던 영상이 영구히 후보에서 빠졌다. 조회 실패는 이번 실행에서만 건너뛰고 상태에 남기지 않으며, **연속 3건 실패하면 레이트리밋으로 보고 중단**한다. 프로브 사이 기본 1초 대기(`--sleep`)도 넣었다

### Changed
- rp-youtube-deck **§영상 흔적 지우기 신설** (`SKILL.md`) — 장표는 게시판에 남아 인용되므로 "어느 유튜브 영상 요약"이 아니라 **공식 문서로 뒷받침되는 독립 기술 문서**로 읽혀야 한다
  - 화면에서 뺀다: 채널명·운영자 이름·영상 제목·공개일·재생시간·유튜브 링크·`[MM:SS]` 타임스탬프·"영상에서는/강의에서/화자" 표현·자막 오인식 같은 제작 과정
  - 남긴다: `<head>`의 `deck-source-video` 메타(화면 비노출·중복 방지용), **원천 문서 링크**(실제 근거), 옵시디언 `sources/`의 전사본·영상 URL(내부 보관)
  - ⛔ **근거를 지우라는 뜻이 아니다** — 타임스탬프를 뺀 자리에 원천 문서 이름을 넣는다. 근거가 안 남으면 원천 검증을 다시 돌리거나 "확인불가"로 귀속을 낮춘다
  - 검증 판정 문구도 맞췄다: `불일치`는 "영상은 A라고 했지만"이 아니라 **"흔히 A로 알려져 있지만 원문은 B"**, `확인불가`는 "화자의 주장"이 아니라 **"공식 문서 근거가 없는 실무 관례"**
  - 워크플로우에 **8단계 영상 흔적 점검**을 넣고 push보다 앞에 뒀다 — 통과 전에는 적재하지 않는다
- rp-youtube-deck **`deck-source-video` 메타를 필수로** (`SKILL.md`) — `rp-youtube-channel`이 이 메타로 "이미 만든 영상"을 판정한다. 빠지면 같은 영상을 다시 고른다
- rp-youtube-deck **구성 표에 0단계(`rp-youtube-channel`) 추가** — 영상 URL을 직접 받았으면 건너뛰는 선택 단계

## [0.27.0] - 2026-08-07

### Added
- **`rp-youtube` 신설** — 유튜브 링크 하나로 자막을 받아 영상 내용을 분석한다. 영상은 37분을 봐야 알 수 있고 다시 찾기도 어렵다는 문제를 타임스탬프 전사본으로 푼다
  - **`scripts/fetch_transcript.py`** — 메타데이터 + 자막 → 타임스탬프 전사본. yt-dlp가 없으면 `~/.rp-youtube/venv`에 자동 설치하고, `--dump-single-json` 한 번으로 메타·자막 트랙 목록을 함께 받는다
  - **클라이언트 폴백** `ios → android → tv → web` — `web`·`tv` 클라이언트는 SABR 강제와 `The page needs to be reloaded`로 자주 실패한다. 실제로 기본 클라이언트가 실패한 영상이 `ios`로는 자막 목록까지 정상 조회됐다
  - **언어 우선순위** 사용자 지정 → 영상 원어(`-orig`) → 한국어 → 영어, 수동 자막 우선. 기계번역본은 용어가 뭉개지므로 원어를 정확히 같은 코드보다 먼저 고른다
  - **접근 게이트 판정** — `availability`가 `subscriber_only`·`premium_only`·`needs_auth`·`private`면 종료 코드 3으로 중단한다. ⛔ 쿠키 추출·계정 우회·미러 탐색 금지 (멤버십 영상에서 메타데이터만 통과하고 자막이 빈 채로 넘어가 "자막 없음"으로 오인되던 경로를 막았다)
  - **§전사본 정규화** — 자동자막은 STT라 기술 용어와 숫자가 깨진다. 깨진 채로 요약하면 그 오류가 결론에 실린다. 실측 사례로 3종 규칙을 고정: 고유명사 복원(`포스트레 SQL`→PostgreSQL, `시카리 CP`→HikariCP, `이틀 스로우`→Little's Law), **숫자 교차검증**(`MySQL 맥스 커넥션 15개`→151, `곱하기 1`→×2, `시작값 26`→16), 문장 정리. 불확실한 복원은 `(추정)` 표기
  - 모든 주장·수치에 `[MM:SS]` 앵커 강제 — 원본 확인 경로를 끊지 않는다
- **`rp-youtube-deck` 신설** — 영상 분석 → 원천 검증 → 장표까지 한 번에. 영상 요약은 화자의 말을 옮기면 끝이지만 장표는 남아서 인용된다는 전제
  - **§원천 검증** — 영상이 인용한 문서를 WebFetch로 직접 열어 대조하고 `일치`/`보강`/`불일치`/`확인불가` 4판정을 붙인다. **원문이 영상보다 우선** — 다르면 원문을 싣고 "영상은 A라고 했지만 원문은 B예요"로 차이를 밝힌다. ⛔ 블로그 재인용으로 원천 대체 금지
  - **§장표 추가 요건** — 표지에 출처 영상(제목·채널·업로드일·길이), 영상發 주장마다 `[MM:SS]`, 마지막에 원천 링크 출처 장, 구어체 전사본을 토스 톤으로 재작성
  - **항상 topic 모드로 취급** — 조사를 동반하므로 옵시디언 ingest가 켜지고, `sources/`에 정규화 전사본과 원천 대조표를 함께 남긴다
  - 오케스트레이터는 매 실행 `rp-youtube`·`rp-deck`의 `SKILL.md`를 실제 Read한다. ⛔ 규칙을 기억으로 재구성 금지

## [0.26.0] - 2026-08-03

### Removed
- rp-deck **독립 리뷰에서 Codex 리뷰어 제거** (`review.md`, `SKILL.md`) — 리뷰어를 Claude 서브에이전트 1인으로 정리한다. `codex exec` 서브프로세스·Bash `timeout: 600000` 강제·병렬 2인 취합·출처 표기(`합의`/`Claude만`/`Codex만`) 규칙이 함께 빠졌다
  - 외부 CLI 설치 여부에 리뷰 성립이 걸리지 않고, 리뷰 결과가 항상 같은 규칙 파일 해석 위에서 나온다

### Changed
- rp-deck **리뷰어 프롬프트를 고정 템플릿으로** (`review.md`) — 기존에는 "이런 걸 전달하라"는 지침만 있어 실행마다 프롬프트가 달라졌고, 리뷰 품질이 그때그때 흔들렸다. §3에 **복사해 쓰는 R1 프롬프트 전문**, §4에 R2용 append 블록을 고정한다
  - **역할·금지 명문화**: "너는 검수자다, 작성자가 아니다" · 파일 수정 금지 · 인상 비평 금지 · 원문에 없는 사실 창작 금지
  - **근거 조항 강제**: 모든 지적에 `design-rules.md §N` / `visual-patterns.md §N`을 찍게 하고, 조항을 못 찍는 지적은 취향으로 보고 버린다
  - **슬라이드 번호 강제**: `<section class="slide">`를 문서 순서로 세어 지적마다 번호를 붙인다
  - **반환 형식 고정**: `{항목#, 심각도, 슬라이드, 문제, 근거 조항, 수정안}` 표만, 서두·총평 없이. 무지적 항목은 `통과: 1,5,7`, 시간 초과분은 `미검토: 8,9`로 구분해 침묵과 통과를 구별
  - **심각도 판정 기준 정의**: high = 사실 오류·주장과 코드 불일치·잘려서 안 보임·독자가 막힘 / med = 규칙 위반이나 이해는 됨 / low = 다듬으면 좋음
  - **R2는 재검증 라운드로**: 채택·보류 목록을 넘겨 같은 지적 반복을 막고, 고친 결과가 실제로 해결됐는지와 수정하며 생긴 새 문제를 보게 한다
- rp-deck **평가 항목 8개 → 10개** (`review.md`) — 0.25.0에서 규칙만 추가되고 리뷰 항목에는 반영되지 않아, 정작 새 규칙을 아무도 검수하지 않던 구멍을 메운다
  - **5. 레이아웃 안전** 신설 — `visual-patterns.md` §7 높이 예산으로 1280×720 초과 장을 번호·추정 높이와 함께 지목 (자주 넘치는 조합 3종 우선 확인)
  - **9. 코드·주장 정합성** 신설 — `design-rules.md` §11 전 항목(복붙 가능성·캡션과 코드 일치·앞뒤 판정 충돌·단정의 반례·사례의 결말·예시 표기)
  - 기존 항목도 판정 가능한 형태로 재서술 — "비전공 이해도"는 멈칫 지점을 슬라이드 번호로, "부가 설명·용어"는 재설명·용어집 유무까지

## [0.25.0] - 2026-07-31

### Changed
- rp-deck **더 쉽고 더 자세하게** (`design-rules.md`, `visual-patterns.md`, `SKILL.md`) — 실제 생성 2건(가상 스레드 핀닝 45장 / StarRocks 쿼리 관리 46장)을 독립 리뷰 2라운드로 돌려 나온 지적을 규칙으로 옮긴다
  - **용어 재설명이 기본으로** (`design-rules.md` §8) — 기존 "10장 이상 뒤에 다시 핵심으로 쓰이면" 조건을 없앤다. 어려운 용어는 **앞에서 설명했더라도 다시 나온 장에서 또 푼다**. 독자는 중간 장부터 볼 수도 있고, 각주는 `position:absolute`라 본문을 밀지 않아 반복 비용이 거의 없다
  - **비교·수치는 표·차트·공식으로** (`design-rules.md` §3) — "A는 ~하고 B는 ~해요" 문장이 나오려 하면 그 자리에서 표로, 비교 가능한 실측치가 2개 이상이면 차트로, 실데이터가 없어도 계산으로 유도되는 값은 `.formula` 한 줄로 옮긴다
  - **가장 어려운 장엔 `.plain` 필수** (`design-rules.md` §0) — 개념 도약이 가장 큰 장을 하나 고르고 거기엔 반드시 쉬운 말 풀이를 붙인다. 리뷰에서 "덱에서 제일 어려운 장에 정작 풀이가 없다"가 두 번 나왔다

### Added
- rp-deck **§11 코드·사례·주장의 정합성** (`design-rules.md`) — 장표 코드는 읽히는 데서 끝나지 않고 복사돼 쓰인다는 전제를 규칙화
  - **복붙 가능한 코드**: 생성자·필드·import가 빠져 컴파일되지 않는 예제 금지. 줄였으면 `.cmt`에 명시 (enum 생성자 누락·미정의 메서드로 두 번 걸렸다)
  - **캡션은 코드가 하는 일만**: 지연 로딩 코드를 싣고 캡션엔 "부팅 때 실패해요"라고 써서 코드가 주장을 반증한 사례
  - **앞뒤 판정 충돌 금지**: 앞 장 매트릭스에서 "못 잡아요"로 판정한 것을 뒤 장에서 해법으로 제시한 사례
  - **단정 앞에 반례 확인** · **사례엔 출처와 결말** · **예시 값은 예시라고**
- rp-deck **§7 높이 예산** (`visual-patterns.md`) — 첫 생성에서 6장이 1280×720을 넘겨 글자가 잘리고 각주와 겹쳤다. 설계 단계에서 쓸 수치가 없던 게 원인
  - **본문 예산**: 각주 있으면 488px, 없으면 576px (`.gloss`는 `position:absolute`라 예산 제외)
  - **요소별 실측 높이표** 16종 — `table` = 40 + 51 + (행수 × 58~63), `.code` = 30 + (줄수 × 29) + 36, `.steps` = 32 + (단계수 × 44) 등
  - **자주 넘치는 조합 4종** — 코드 블록 2개 + 각주는 거의 항상 초과, `.two-col` 안의 `.code`는 긴 쪽에 맞춰 양쪽이 늘어남, 패널 안 코드 한 줄은 47자 이내
  - **검증 방법** — 직계 자식 높이 합 + 패딩을 720과 비교. `margin-top:auto`인 `.kicker`는 남는 공간을 흡수하므로 마무리 장의 근접값은 실제 초과가 아님
- rp-deck **자가 린트 8항목 추가** (`design-rules.md` §10) — 위 규칙에 대응하는 체크 항목

## [0.24.0] - 2026-07-30

### Added
- rp-deck **장표 삭제 모드** (`scripts/delete_deck.py`, `SKILL.md`) — 올린 덱을 내리는 일이 생성만큼 잦은데 매번 손으로 파일을 지우고 인덱스를 고쳐야 했다
  - **대상 지정 3종**: 라이브 URL(퍼센트 인코딩 자동 해제) · 대상 루트 기준 상대 경로 · 제목/경로 키워드. 여러 건 동시 지정
  - **확인 1회**: 기본은 dry-run으로 지울 제목·경로만 출력, 사용자 동의 후 `--yes`로 실제 삭제
  - **모호·미매칭이면 아무것도 안 지운다** — 후보를 출력하고 종료 코드 2 (키워드 오매칭으로 엉뚱한 덱이 날아가는 사고 차단)
  - 삭제 후 **빈 디렉터리 정리 + index.html 재생성**(`update_index.py`의 스캔·렌더 재사용)까지 한 번에
  - 옵시디언 `sources/`·`wiki/` 노트는 남긴다 (덱=시각 요약 / vault=원본 근거로 역할 분리)

## [0.23.0] - 2026-07-30

### Removed
- rp-deck **휠 슬라이드 이동 제거** (`template.html`) — 0.21.0~0.22.1에서 넣은 wheel 핸들러를 걷어낸다. 감도를 어떻게 잡아도 문서 스크롤 습관과 부딪혀, 넘길 의도가 없을 때 장이 움직이는 쪽이 더 거슬린다는 판단
  - 남는 이동 수단: 키보드(←→·Space·Home/End·F) · 터치 스와이프 · 좌우 클릭 영역
  - `#hint` 문구 `← → · Space · F 전체화면`으로 복귀
  - `SKILL.md`의 "네비게이션 4종 필수" 항목 삭제
  - document-center 기존 덱 24개도 동일하게 제거

## [0.22.1] - 2026-07-30

### Fixed
- rp-deck **트랙패드로 살살 굴리면 아무 반응이 없던 문제** (`template.html`) — 누적 문턱이 280px 하나뿐이라 가벼운 스와이프(총 60~200px)가 그대로 묻혔다
  - 제스처의 **첫 장만 문턱 60px**(`WFIRST`), 두 번째 장부터 280px(`WSTEP`) — 살살 굴려도 한 장은 반드시 넘어가고, 확 내리면 굴린 만큼 그대로
  - 손가락만 얹었을 때의 떨림(총 40px 수준)은 여전히 무시
  - 세로·가로 동일 적용. 살살 60px → 1장 / 살짝 304px → 1장 / 보통 1,374px → 6장 / 확 4,713px → 17장

## [0.22.0] - 2026-07-30

### Changed
- rp-deck **휠 이동을 "굴린 만큼"으로 변경** (`template.html`) — 0.21.0의 관성 흡수 잠금(1회 플릭=1장)을 걷어내고 입력량에 비례시킨다
  - **마우스 눈금 1개 = 1장** — `|delta| ≥ 50` + 직전 이벤트와 `40ms` 이상 간격이면 눈금으로 보고 즉시 한 장. deltaY가 100이든 120이든 동일
  - **트랙패드 등 이어지는 스크롤 = 누적 280px마다 한 장** — 확 내리면 그만큼 여러 장
  - 방향 전환·400ms 이상 정지 시 누적 잔량 폐기, `deltaMode`(line/page) 환산 처리
  - 실측 대응 이동량: 살짝 1장 · 보통 5장 · 확 17장 · 마우스 5눈금 5장 (`WSTEP` 상수 한 줄로 조절)

### Added
- rp-deck **가로 휠·트랙패드 좌우 스와이프로 장 넘기기** (`template.html`) — 세로와 동일 로직, `|deltaX| > |deltaY|`면 가로 축을 따른다
  - 축이 섞여 들어와도 **움직임이 큰 축**만 반영 — 세로 스와이프에 가로 흔들림 30%가 껴도 세로로만 이동
  - **뒤로가기 제스처 차단**: 무대가 `overflow:hidden`이라 가로 스와이프가 브라우저 history 뒤로가기로 새던 문제. 리스너를 `{passive:false}`로 바꾸고 `preventDefault()`
  - 핀치 줌(`e.ctrlKey`)은 가로채지 않고 브라우저에 양보

## [0.21.0] - 2026-07-30

### Added
- rp-deck **휠 슬라이드 이동** (`template.html`) — 마우스 휠·트랙패드로 다음/이전 장 이동
  - **트랙패드 관성 흡수**: 한 번 넘기면 잠금, 휠 이벤트가 120ms 조용해질 때까지 + 최소 450ms 대기 후 해제. 한 번 튕겨 관성 이벤트가 수십 개 와도 딱 한 장만 넘어간다
  - `|deltaY| < 8`은 미세 스크롤로 보고 무시, `{passive:true}`(무대가 `overflow:hidden`이라 페이지 스크롤과 경합 없음)
  - `#hint` 문구 `← → · 휠 · Space · F 전체화면`
  - 트레이드오프: 휠 이벤트가 120ms 미만 간격으로 **끊김 없이 계속** 들어오면(고속 연속 스크롤) 잠금이 유지돼 한 장에 머문다. 손을 떼면 120ms 뒤 해제 — 관성 오버런을 막는 대가로 수용
  - `SKILL.md` 워크플로우 6단계에 "네비게이션 4종 필수(키보드·스와이프·클릭·휠)" 명시, 보강 시 휠 없는 기존 덱에 주입하도록 규정

## [0.20.1] - 2026-07-30

### Changed
- rp-deck **옵시디언 vault 질문을 평생 1회로 제한** — 거절 응답을 `.rp-deck.json`에 `"obsidian": false`로 저장해 topic 모드 실행마다 되묻던 동작 제거
  - `obsidian` 값 3분기: 문자열=적재 / `false`=거절 확정(묻지 않고 스킵) / 키 없음=1회 질문
  - 거절·무응답 시 `false` **즉시 저장**(설정 파일이 없으면 새로 생성) — ⛔ 저장 생략 금지
  - 되돌리기는 사용자가 명시로 vault 적재를 요청할 때만(그때 경로를 물어 `false` 덮어쓰기)
  - `repo`의 재질문 정책은 변경 없음(적재 대상이라 매 실행 확인 유지)

## [0.20.0] - 2026-07-27

### Added
- **`rp-pr-review` 스킬 신설** — PR 링크 하나로 독립 리뷰어 둘에게 병렬 코드 리뷰를 시키고 중요순 리스트로 정리
  - 리뷰어: **Claude 서브에이전트(Agent 툴) ∥ Codex 서브프로세스(`codex exec`)** — 같은 턴에 동시 호출, 서로의 결과를 모름
  - **기준 주입 없음** — 두 리뷰어에게 `"<PR URL> 코드 리뷰 해줘"` + 반환 형식(`심각도 / 파일:라인 / 문제 / 수정안`)만 전달. 체크리스트를 주지 않아 시각이 갈리게 둔다
  - PR 내용은 각 리뷰어가 `gh` CLI로 직접 가져옴 — 사내 GitHub Enterprise 링크도 그대로 동작
  - **병합**: 같은 파일·같은 근본 원인이면 한 항목으로 합치고 심각도는 높은 쪽, 출처는 `합의`/`Claude만`/`Codex만`로 표기. 단독 지적을 버리지 않음
  - **정렬**: 심각도(Critical→Low) → 합의 우선 → 파일 경로순
  - **건당 10분**(Codex는 Bash `timeout: 600000`, Claude는 프롬프트 상한 명시). 초과 시 1줄 기록 후 비차단 스킵, ⛔ 재시도·연장 없음
  - **읽기 전용**: 파일 생성 금지(터미널 출력만), PR 코멘트·머지·코드 수정 전부 금지, 1라운드
  - 대상은 **사용자가 준 링크만** — 현재 브랜치·git remote·대화 맥락으로 유추 금지

## [0.19.0] - 2026-07-27

### Added
- rp-deck **시각 컴포넌트 6종** (`template.html`) — 텍스트 위주 장표를 그림으로 바꾸기 위한 전용 표현 수단
  - `.schema` 스키마 카드 — 테이블 구조를 `키 뱃지(PK/FK/IDX) / 컬럼명 / 타입 / 쉬운 설명`으로 시각화
  - `.dataflow` 데이터 흐름 — 홉마다 **그 시점의 실제 데이터 모양**(JSON·SQL 조각)과 하는 일을 함께 표시
  - `table.diff` 레코드 변화 — Before/After 행 대비, 바뀐 셀만 `.chg` 강조(안 바뀐 칸도 유지)
  - `table.matrix` 비교 매트릭스 — 행은 **상황**, 셀은 `yes/mid/no` 판정어로 "어떤 상황에 무엇이 맞는지"
  - `.layers` 계층 구조 — 사용자와 가까운 순으로 쌓인 층 + 층마다 하는 일 한 줄
  - `.plain` 쉬운 말 풀이 — 전문 설명 바로 아래 일상 비유 한 문장
  - `.slide.has-gloss` — 각주를 쓰는 슬라이드의 하단 여백 확보(각주 4줄까지 본문과 겹치지 않음)
  - 데모 슬라이드 5장 추가(16 → 21장), 카운터 동기화
- rp-deck [`reference/visual-patterns.md`](rp-plugins/skills/rp-deck/reference/visual-patterns.md) 신설 — **주제 → 시각 표현 매핑 SSOT**
  - 14개 주제 유형별 컴포넌트 매핑표 + 컴포넌트별 사용 규칙
  - 골든 패턴 2종: "데이터 저장 방식"은 스키마 → 흐름 → 값 변화 **3장 세트**, "기술 A vs B"는 소개 → 매트릭스 → 처리 흐름 → 수치 **4장 세트**
  - 밀도 게이트: 그림 없는 슬라이드가 **연속 3장** 초과 금지

### Changed
- rp-deck **독자 기준을 비전공자로 재정의** (`design-rules.md` §0 신설) — 개발자만이 아니라 **기획자·비전공 대학 4학년**이 읽어도 이해되게
  - 사전지식 0 가정 / 용어는 첫 등장 자리에서 풀기 / 쉬운 말 먼저·정확한 말 다음 / "왜"가 "어떻게"보다 앞 / 분량보다 이해
- rp-deck **헤드라인에 대상 이름 필수** (`design-rules.md` §2) — 제목만 봐도 주제가 잡히게
  - "응답이 40% 빨라졌어요"(X) → "Redis 캐시를 거치니 응답이 40% 빨라졌어요"(O), "Redis와 Kafka 비교"(X, 명사 라벨) → "Redis와 Kafka는 어울리는 자리가 달라요"(O)
  - 대명사·추상어("이것","성능","구조")만으로 끝내기 금지. 길이 권장치 ≤20자 → ≤24자(이름 넣느라 길어지는 건 허용)
- rp-deck **용어 각주 확대** (`design-rules.md` §8) — 슬라이드당 1~3개·2줄 → **2~5개(필요하면 더)·4줄**, 10장 이상 뒤 재등장 시 재설명 허용, 한 줄로 안 끝나는 개념은 `.plain`·소개 장으로 승격
- rp-deck `design-rules.md` §3을 시각화 원칙만 남기고 컴포넌트 선택은 `visual-patterns.md`로 이관, 자가 린트 체크 7종 추가
- rp-deck 독립 리뷰 평가 항목 7 → 8개 (`review.md`) — **"비전공 이해도"** 항목 신설(멈칫 지점을 슬라이드 번호로 지적), 시각 요소 항목에 매핑표 준수·밀도 게이트 반영, 헤드라인 대상 이름 검증 추가

### Fixed
- rp-deck **내용이 넘칠 때 표·차트 행이 소리 없이 잘리던 문제** (`template.html`) — `.slide`가 flex 컨테이너라 자식이 축소되고 `overflow:hidden`에 잘려, 5행짜리 표가 4행만 보이는데도 경고가 없었다. `.slide>*{flex:none}`으로 축소를 막아 넘치면 눈에 보이게 바꿈
- rp-deck 선차트 슬라이드가 720px를 31px 초과하던 레이아웃 (`template.html`) — `.linechart` 상단 여백 40 → 20px, SVG `max-height:340px` 적용. 전 21장이 1280×720 안에 들어가는 것을 헤드리스 렌더로 검증
- rp-deck 스키마 카드에서 키가 없는 컬럼에 빈 회색 뱃지가 남던 문제 — `.sc-row .key:empty{background:none}`

## [0.18.0] - 2026-07-24

### Changed
- 독립 리뷰 **호출 1건당 타임아웃 10분(600초)** — rp-deck·rp-post 공통. 리뷰가 무한정 늘어져 파이프라인을 잡아먹는 것을 차단
  - Codex: Bash 툴 `timeout: 600000`(ms)로 하드 강제 (⛔ 값 상향 금지)
  - Claude 서브에이전트: Agent 툴에 타임아웃 인자가 없으므로 프롬프트에 "10분 내 반환" 상한 명시(미완이면 그 시점까지의 평가만 반환) + 메인이 초과 판단 시 결과 없이 진행
  - 적용 단위는 **건당** — 합산 아님. rp-deck은 R1/R2 × Claude/Codex 각각 10분
  - 초과 시 **비차단 즉시 스킵**(⛔ 재시도·연장 없음), 1줄 로그(`타임아웃[Codex-R2]: 600s 초과, 스킵`) 후 남은 리뷰어 결과로 진행. 양쪽 다 타임아웃이면 그 라운드는 지적 0건 처리(rp-post는 리뷰 요약에 명시)

## [0.16.0] - 2026-07-17

### Changed
- rp-deck 게시판 index — 좌우 2-pane 독립 스크롤 + 모바일 세로 레이아웃 (`update_index.py`)
  - 데스크톱: 100dvh 고정, 좌측 트리 카드·우측 리스트 카드가 각각 내부 스크롤, 리스트 헤더 sticky, 페이지 자체 스크롤 없음
  - 모바일(≤860px): 2-pane 해제·페이지 전체 스크롤, 행을 제목·설명·날짜 세로 스택(날짜 좌측 정렬·설명 2줄), 컬럼 헤더 숨김, 좌우 패딩 대칭(16px)
  - 트리·리스트 pane 스크롤바 auto-hide — idle 시 투명, scroll 이벤트 700ms 동안·호버 시 노출 (`scrollbar-width/-color` + `::-webkit-scrollbar` 병행)

## [0.15.0] - 2026-07-16

### Changed
- rp-deck `update_index.py` 생성기를 카드 그리드 → **게시판 레이아웃**으로 교체
  - 좌측: 독립 "전체" 버튼 + 뎁스별 중첩 디렉토리 트리(개수 뱃지·접기/펼치기)
  - 우측: 제목·작성일 게시판 리스트, 경로 태그 클릭 시 해당 디렉토리 필터
  - 우상단 검색(0.5s debounce), 트리/전체/태그 선택 시 검색어 초기화. 라이트/다크 테마, 모바일 디렉토리 토글
  - 덱 데이터는 원본 그대로 JSON 임베드, 트리·필터·검색은 클라이언트 렌더 (stdlib only)

### Fixed
- 경로 원본 유지 + `<script>` 안전성은 `<`·`>`·`&`·U+2028/9 유니코드 이스케이프로 확보, DOM 주입은 클라이언트 `esc()` 처리 → `R&D`·`Q&A` 등 특수문자 폴더 필터 정합
- 비 UTF-8 덱 파일은 건너뛰어 전체 빌드 중단 방지
- 디렉토리 노드 조회를 `querySelectorAll`+`find`로 교체해 따옴표 포함 폴더명 크래시 제거

## [0.14.0] - 2026-07-16

### Added
- rp-deck 홈 버튼 — `.rp-deck.json`에 `home`(게시판 홈 절대 URL)이 있으면 각 장표 우하단 카운터 옆에 토스 스타일 홈 pill(인라인 SVG 아이콘 + "홈", 같은 탭 이동)을 넣어 분류 폴더 깊숙한 덱에서 게시판 홈으로 한 번에 복귀
  - `template.html`: `#hud-br`(우하단 flex) + `#home` 컴포넌트, 홈 클릭이 슬라이드 넘김으로 새지 않는 클릭 가드
  - 출처 규칙은 repo·vault와 동일(`.rp-deck.json` 또는 사용자 명시만, 추측 금지). 없으면 홈 버튼 없이 생성(묻지 않음)
  - 적용 범위: 이 스킬이 만지는 덱(신규·보강). 기존 덱 일괄 백필 없음

## [0.13.0] - 2026-07-13

### Added
- `rp-codebase-recap` 스킬 — 특정 레포(로컬·GitHub)를 심층 발굴 분석해 프로젝트 파악 리포트를 터미널에 출력
  - 5-pass 절차(광역 스캔 → 핵심 서브시스템 선정 → 서브에이전트 병렬 정독 → 문제해결 케이스 스터디 재구성 → 종합)로 실제 코드를 정독, 커밋 제목 요약 대체 금지
  - 작성자 스코프 분석(지정 작성자 커밋/코드), 대상은 로컬 경로·레포 이름·GitHub URL 유동 인식(GitHub는 scratchpad clone)
  - 리포트 10섹션: 개요·기술스택·아키텍처·역할/기여도·담당영역·문제해결(케이스 스터디)·정량수치·도메인기능·리스크·발굴노트
  - 규칙: 레포 비종속(특정 레포 특화 금지)·근거 필수(파일:라인)·설명 눈높이(비전공자 이해 가능·용어 풀이)·최대 발굴(불확실은 `(추정)`)·민감정보 금지

## [0.12.0] - 2026-07-10

### Changed
- rp-post 독립 리뷰 2라운드 → **단일 라운드** — Claude ∥ Codex 병렬 각 1회 → 수정 → 종료 (수정 후 재리뷰 없음, 지적 0건이면 수정 생략)

## [0.11.0] - 2026-07-10

### Added
- rp-post 독립 리뷰 루프 (`reference/review.md`) — 선택된 최종본 대상, 작성자 셀프리뷰 금지
  - Claude 서브에이전트 ∥ Codex `codex exec` 병렬 2인 리뷰, R1 → 수정 → R2 → 수정 → 종료 (상한 2라운드·조기 종료·Codex 불가 시 비차단)
  - 평가 7항목: 사실 정확성 · 아키타입 공식 준수 · 체크리스트 독립 재채점 · 플랫폼 적합성 · 민감 정보(즉시 high) · 독자 전달력 · AI 문체 냄새
  - high·med 반영, low 로그. 충돌 시 `patterns.md` 실측 우선. 톤 보존 원칙(사용자 개인 톤 획일화 금지)
- SKILL.md 워크플로우 8~10단계 재구성 (리뷰 루프 → 마무리 출력+리뷰 요약 → 저장) + 절대 규칙 "셀프리뷰 금지"

## [0.10.0] - 2026-07-10

### Added
- rp-post 사내 지식 공유 카테고리
  - `platforms.md` company 프로파일 — 목표=전달력·재사용성(바이럴 지표 아님), **존댓말**, 채널 2형(슬랙 공유형 ≤300자+링크 / 위키 문서형)
  - `archetypes.md` K계열 아키타입 3종 — K1 트러블슈팅 회고 · K2 TIL/개념 정리 · K3 의사결정 공유(ADR 스타일)
  - `checklist.md` 사내 공유용 체크리스트 7항목 (TL;DR·맥락·재현성·근거·다음 액션·존댓말·민감 정보) + K계열 실패 패턴 3종

### Changed
- rp-post SKILL.md — 목표에 "지식 공유(사내)" 분기, 사내 모드에 A계열(바이럴 훅) 사용 금지, 톤 분리 규칙(SNS=플랫폼 관례 / 사내=존댓말)

## [0.9.0] - 2026-07-10

### Added
- `rp-post` 스킬 (초안) — 실측 바이럴 패턴 기반 글 초안 생성
  - 쓰레드 IT/개발 인기글 150개 실측 분석 내장 (상하위 4분위 비교·길이 구간·지표별 매트릭스, `reference/patterns.md`)
  - 아키타입 6종 공식(`archetypes.md`) → 변형 초안 2~3개 생성 + 7항목 체크리스트 검증(`checklist.md`)
  - 플랫폼 프로파일(`platforms.md`): threads(실측)·blog·naver-cafe·기타 + 플랫폼 간 변환 규칙 + 데이터 갱신 절차
  - 절대 규칙: 자동 게시 금지 · 사실 날조 금지(`[숫자 채우기]` 플레이스홀더) · 민감정보 금지

## [0.8.0] - 2026-07-08

### Added
- rp-deck 독립 에이전트 병렬 리뷰 루프 (`reference/review.md`) — 2라운드, 작성자 셀프리뷰 금지

### Changed
- rp-deck 시각 요소 강화 — 선차트(추세)·도넛(구성비) 컴포넌트 추가

## [0.7.0] - 2026-07-02

### Added
- 용어집(`terms`) 컴포넌트 — 용어 많은 덱은 마무리 직전 "용어 사전" 슬라이드로 핵심 용어를 2열 정리 (template 데모 포함)

### Changed
- 깊이 강화(§9): 복잡한 메커니즘은 **개요 흐름 1장 + 심화 여러 장**, 거쳐가는 **중간 컴포넌트·단계 생략 금지**(A→Z 뭉치기 금지)
- 용어 각주(§8): 상한 완화(슬라이드당 1~5개+, 최대 3줄), 낯선 용어는 웬만하면 다 설명
- SKILL.md 구조 설계 단계·자가 린트 반영

## [0.6.1] - 2026-07-02

### Changed
- 대상 레포는 `.rp-deck.json` 또는 이번 실행 명시 입력으로만 지정 — git remote·대화 맥락·추측 등 **어떤 유추·자동 지정도 금지**. 명시 입력 없으면 로컬 모드
- SKILL.md에서 예시 레포명(document-center) 제거

## [0.6.0] - 2026-07-02

### Changed
- 레포 모드: **작업 시작 전 원격 강제 최신화** 규칙화 — `git fetch && git reset --hard origin/<기본브랜치>`로 캐시를 원격 SSOT에 일치시킨 뒤에만 기존 문서 확인·생성 시작 (stale 캐시로 보강/신규 오판 방지)
- 각 실행은 커밋 후 **push 성공까지 확인**, 실패 시 중단·보고 (로컬에만 커밋 남기지 않음)

## [0.5.0] - 2026-07-02

### Added
- 생성 전 기존 문서 확인 → **보강 vs 신규** 자동 판단
  - 대상 루트(레포 캐시/로컬)의 title·category·summary 종합 + 내용으로 같은 주제 여부 판단
  - 같은 주제 명확 → 기존 파일 그 자리 편집(슬라이드 추가·수정, date만 갱신, 파일명·경로 유지)
  - 새 주제 명확 → 신규 생성 / 애매 → 사용자에게 보강·신규 확인
  - SKILL.md §보강 vs 신규 + 워크플로우 4단계 추가 + 절대 규칙 반영

`rp-plugins` 플러그인 버전 이력. [SemVer](https://semver.org/lang/ko/) 준수.
스킬 변경 시 `rp-plugins/.claude-plugin/plugin.json`의 `version`, **`README.md`의 "현재 버전"**, 이 파일 **세 곳을 함께 갱신한다.** (⛔ 한 곳만 올리면 표기가 갈린다 — 0.15.0·0.16.0에서 실제 발생)

## [0.4.0] - 2026-07-02

### Added
- 타깃 GitHub 레포 적재 — 생성 문서를 환경별로 지정한 레포에 push
  - 프로젝트 루트 `.rp-deck.json`(`{"repo":"owner/name"}`)에 대상 저장
  - 미등록 환경이면 대상 레포를 한 번 물어봄. 등록하면 캐시(`~/.rp-deck/repos/<owner>__<name>`) clone/pull → 4레벨 배치 → index 갱신 → commit·push
  - **미등록·거부 시 로컬 모드**: `<cwd>/docs/decks`에 생성·인덱스만, push 없음 (생성은 항상 수행). 묻지 않고 임의 레포 push 금지
  - SKILL.md 워크플로우(타깃 레포 확인 단계 추가)·절대 규칙 반영

### Changed
- 문서·매니페스트 노출 텍스트에서 조직명 표기 제거 (GitHub 링크 경로는 유지)

### Fixed
- SKILL.md 자가 린트 참조 섹션 번호 정정 (§8 → §10)

## [0.3.0] - 2026-07-02

### Added
- 심화 컴포넌트를 `template.html` SSOT에 정식 등록 (데모 슬라이드 포함)
  - `단계(steps)` — 동작·시나리오 분해, `key`(파랑)·`bad`(빨강)·`ok`(초록) 변형
  - `코드블록(code)`·인라인 `mono`, `공식강조(formula)`
- `design-rules.md` §9 "깊이와 분량" — 이해에 필요한 내용은 줄이지 않기, 복잡도에 맞게 슬라이드 확장, 메커니즘은 단계로, 과소·과대 지양 (린트 항목 추가)
- SKILL.md 구조 설계 단계에 컴포넌트·깊이 지침 반영

## [0.2.0] - 2026-07-02

### Added
- 용어 각주(`.gloss`) — 낯선 전문용어를 해당 슬라이드 하단에 짧게 설명
  - 슬라이드당 1~3개, 약어는 풀텍스트 병기(예: `TTL(Time To Live)`), 한 줄 `·` 구분
  - `template.html` 컴포넌트 + `design-rules.md` §8 규칙 + 자가 린트 항목 추가
  - SKILL.md 워크플로우·절대 규칙에 반영

## [0.1.0] - 2026-07-02

### Added
- `rp-deck` 스킬 — 작업 산출물·문서·주제를 토스 스타일 HTML 슬라이드 장표로 변환
  - 입력 3모드 (컨텍스트 / 파일 / 주제·deep-research)
  - 의존성 없는 단일 HTML, 슬라이드형 네비게이션 (키보드·스와이프·진행바)
  - `docs/decks/` 4레벨 자동 분류 저장 + `index.html` 자동 갱신
  - 디자인·전달력 규칙 SSOT (`reference/template.html`, `reference/design-rules.md`)
  - 인덱스 생성 스크립트 (`scripts/update_index.py`, stdlib only)
