---
name: rp-deck
description: 작업 산출물·문서·주제를 토스 스타일 HTML 슬라이드 장표로 변환. 의존성 없는 단일 HTML(슬라이드형)로 생성하고, 환경별로 지정한 GitHub 레포(owner/name 또는 사내 호스트 전체 URL)에 4레벨로 자동 분류 적재 후 index.html을 갱신한다. 주제 조사(topic) 시 조사 원본·정리 노트를 옵시디언 vault(설정 시)에도 남긴다. "장표 만들어줘", "덱으로 정리해줘", "deck", "슬라이드로 만들어줘", 작업/회고/PRD/문서를 보기 좋게 정리해 달라고 할 때 사용.
---

# rp-deck — 토스 스타일 장표 생성

문서는 잘 안 읽힌다. 핵심을 한 장에 한 메시지로, 토스 느낌의 깔끔한 HTML 슬라이드로 바꿔 3초 안에 이해되게 만든다.

## 레퍼런스 (반드시 먼저 Read)

| 파일 | 역할 |
|------|------|
| [`reference/template.html`](reference/template.html) | 디자인·컴포넌트·네비게이션 SSOT. CSS·클래스를 그대로 재사용하고 내용만 채운다 |
| [`reference/design-rules.md`](reference/design-rules.md) | 구조·전달력·데이터시각화·토스 톤·자가 린트 규칙 |
| [`scripts/update_index.py`](scripts/update_index.py) | 덱 트리 스캔 → index.html 생성 (stdlib only) |

## 입력 모드 (인자로 자동 판단)

| 인자 | 모드 | 동작 |
|------|------|------|
| 없음 | **컨텍스트** | 방금 끝낸 작업/대화를 요약해 장표화 |
| 파일 경로 (`.md` 등 존재하는 경로) | **파일** | 그 문서를 읽어 장표화 |
| 그 외 텍스트 | **주제** | `deep-research` 스킬로 조사 후 장표화 |

## 타깃 레포 (문서 적재 대상)

생성한 문서는 **환경별로 지정한 GitHub 레포**에 적재할 수 있다. 사용 환경이 다르므로, 등록 전에는 대상 레포를 물어본다. **등록은 선택이며, 없으면 로컬에만 생성한다(생성은 항상 해준다).**

- 설정 파일: 실행 프로젝트 루트 `.rp-deck.json` — `{"repo": "...", "obsidian": "..."}`. `repo`는 **`owner/name`(github.com) 또는 전체 URL**(사내 GitHub Enterprise 등 다른 호스트)을 모두 허용. `obsidian`은 vault 경로(선택, §옵시디언 지식베이스).
- **대상 레포 출처는 오직 둘뿐**: (a) `.rp-deck.json`, (b) 이번 실행에서 사용자가 명시한 답. ⛔ cwd의 git remote·이전 대화·이 문서의 표현·추측 등 **어떤 방법으로도 레포를 유추·자동 지정하지 않는다.** 명시 입력이 없으면 무조건 로컬 모드.
- **레포 모드 (파일 있음)**: 캐시 `~/.rp-deck/repos/<owner>__<name>`를 **작업 시작 전 원격 최신으로 강제 동기화**한 뒤 문서를 배치·커밋·push한다.
  - **호스트 일반화**: `repo`가 전체 URL이면 **그 URL로** clone/fetch(`git clone <URL>`), `owner/name`이면 github.com(`gh repo clone`). ⛔ 호스트를 github.com으로 가정하지 않는다. 캐시 경로는 URL/슬러그에서 `<owner>__<name>`을 파싱해 `~/.rp-deck/repos/<owner>__<name>`로.
  - 없으면 clone. 있으면 `git fetch origin && git reset --hard origin/<기본브랜치>` (기본브랜치는 `git remote show origin`으로 확인, 보통 main).
  - 캐시는 **원격 SSOT의 읽기 사본** — reset으로 로컬 변경은 버려진다. 그러므로 **각 실행은 커밋 후 push까지 반드시 완료**하고, push 실패 시 중단·보고한다(로컬에만 남기지 않음).
- **미등록 (파일 없음)**: 대상 레포(`owner/name`)를 한 번 물어본다.
  - 알려주면 `.rp-deck.json`에 저장하고 레포 모드로 진행.
  - **원치 않거나 답이 없으면 → 로컬 모드**: `<cwd>/docs/decks/`에 생성·인덱스 갱신만, 푸시 없음. 저장하지 않으므로 다음 실행에서 다시 묻는다.
- **묻지 않고 임의 레포에 push 금지.**

## 옵시디언 지식베이스 적재 (topic 모드 전용)

topic(주제) 모드는 조사(`deep-research`·웹 검색)를 한다 — 그 **원본 조사 자료를 버리지 않고** 옵시디언 vault에 남긴다. 덱=시각 요약, vault=원본 근거+정리 지식으로 역할을 나눈다.

- **적용 조건**: **topic 모드에서만.** 컨텍스트·파일 모드는 새 조사가 없으므로 건너뛴다.
- **vault 출처는 레포 타깃과 동일 규칙**: `.rp-deck.json`의 `obsidian` 또는 이번 실행의 사용자 명시 답, 이 둘로만. ⛔ 추측·자동 지정 금지. 없으면 **한 번 물어보고**, 알려주면 저장 후 진행, 원치 않으면 **스킵**(덱은 그대로 생성).
- **⛔ vault의 컨벤션을 따른다 (rp-deck 임의 폴더·형식 금지)**: vault 루트에 `CLAUDE.md`(또는 `README`)가 있으면 **먼저 Read**하고 그 스키마·페이지 형식·폴더 규칙을 그대로 준수한다. rp-deck 고유의 폴더/프론트매터를 강요하지 않는다.
- **동작 = vault의 Ingest 플로우** (스키마에 정의가 있으면 그 정의 우선, 없으면 아래 기본):
  1. **원본 보존**: 조사 원문(deep-research 결과 + 수집 발췌·출처 URL)을 `sources/`에 새 파일로 저장. ⛔ 기존 `sources/` 파일은 수정·삭제하지 않는다(출처 무결성).
  2. **정리 페이지**: 핵심을 `wiki/<도메인>/<토픽>.md`로 생성/갱신(1개념=1페이지). 도메인은 **덱 카테고리와 대응**(backend/spring·data·infra·frontend·mobile·testing …).
  3. **연결**: 관련 페이지에 `[[위키링크]]`로 상호 연결.
  4. **카탈로그·로그**: `index.md` 갱신, `log.md` 맨 위에 `## [YYYY-MM-DD] ingest | 제목` append.
- **보강 우선**: vault에 같은 토픽 페이지가 있으면 새로 만들지 말고 갱신한다(모순은 명시).
- **⛔ 민감정보 금지**: 내부 호스트·포트·인증서 경로·IP·시크릿은 vault에 기록하지 않는다(동기화 vault 유출 방지).

## 보강 vs 신규

생성 전, 대상 루트에 **같은 주제 문서가 이미 있는지** 확인해 보강할지 새로 만들지 판단한다.

- 판단 근거: 기존 덱의 `title` + `category`(분류 경로) + `summary`(meta) **종합 유사도**, 그리고 내용까지 보고 최종 결정. 파일명·경로 단독 일치만으로 판단하지 않는다.
- **보강**: 같은 주제가 명확할 때. 기존 파일을 그 자리에서 편집(슬라이드 추가·수정), meta date만 오늘로, 파일명·경로 유지.
- **신규**: 새 주제가 명확할 때. 새 파일 생성.
- **애매**(부분 겹침 등): 사용자에게 보강/신규를 물어본다. 명확하면 묻지 않고 자동 진행하되, 어느 쪽인지 진행 중 한 줄로 알린다.

## 워크플로우

1. **타깃 레포 확인·최신화** — `.rp-deck.json`을 읽는다.
   - 있으면 캐시를 **원격 최신으로 강제 동기화**(`git fetch origin && git reset --hard origin/<기본브랜치>`, 캐시 없으면 `git clone`) → **레포 모드**. ⛔ 이 동기화 전에는 기존 문서 확인·생성을 시작하지 않는다.
   - 없으면 대상 레포를 한 번 물어본다. 알려주면 저장 후 레포 모드, 원치 않으면 **로컬 모드**(푸시 없음)로 진행. (§타깃 레포)
2. **레퍼런스 로드** — `template.html` + `design-rules.md`를 Read한다. (매 실행마다)
3. **입력 수집** — 모드 판별 후 원문 확보. 주제 모드는 `deep-research` 실행 결과를 원문으로 삼고, **조사 원본을 옵시디언에 남기므로 vault 타깃도 확인**한다(§옵시디언; 설정 없으면 한 번 물어봄, 원치 않으면 스킵).
4. **기존 문서 확인 (보강/신규)** — 대상 루트(레포 캐시 또는 `<cwd>/docs/decks`)의 `index.html`·기존 덱 메타(title·category·summary)를 스캔해 같은 주제 문서가 있는지 본다. (§보강 vs 신규)
   - 같은 주제가 **명확**하면 → 그 파일을 **보강** 대상으로.
   - 새 주제가 **명확**하면 → **신규**.
   - **애매하면** 사용자에게 보강/신규를 물어본다. (명확하면 자동, 결정은 다음 단계 진행 중 한 줄로 알림)
5. **구조 설계 (자동)** — "이 독자에게 어떻게 하면 가장 잘 전달될까"를 먼저 설계한다.
   - 결론 먼저(Minto), 도입은 SCQA, 목차·섹션은 주제 라벨이 아닌 **주장**으로.
   - 한 장 = 한 메시지. 주장이 2개 이상이면 장을 나눈다.
   - 각 장에 맞는 컴포넌트 선택: 큰숫자 KPI / 요약카드 / 2단대비 / 표 / 막대차트 / 플로우 / 단계(steps) / 코드블록 / 공식강조 / 용어집(terms) / 한줄강조.
   - 실데이터가 있을 때만 차트, 예시값은 각주 명시.
   - 보강 모드면 기존 슬라이드는 두고 **추가·수정할 지점만** 설계한다.
   - **깊이·분량**: 이해에 필요한 내용은 줄이지 않는다. 주제 복잡도에 맞게 슬라이드를 늘리고, 동작·실패 과정은 단계(steps)로 분해(위험=bad/해소=ok/핵심=key), 근거·수치·코드·시나리오·흔한 오해를 충분히 담는다. 단, 뻔한 내용으로 장 수만 늘리지 않는다. ([`design-rules.md`](reference/design-rules.md) §9)
   - **개요 + 심화 2단**: 복잡한 메커니즘은 개요 흐름 한 장 뒤에 심화 여러 장으로 쪼갠다. 흐름의 **중간 컴포넌트·단계를 생략하지 않는다**(A→Z로 뭉치기 금지).
   - **용어**: 낯선 용어는 하단 각주로 넉넉히 풀고(슬라이드당 1~5개+), 용어 많은 덱은 **마무리 직전 "용어 사전"(terms) 슬라이드**를 둔다. ([`design-rules.md`](reference/design-rules.md) §8)
6. **HTML 생성 / 보강** —
   - **신규**: `template.html`의 구조·CSS·JS를 그대로 쓰고 슬라이드 내용만 채운다.
   - **보강**: 기존 파일을 **그 자리에서 편집**해 슬라이드를 추가·수정한다. meta의 date만 오늘로 갱신, **파일명·경로 유지**.
   - 헤드라인은 완결 주장문(해요체), 카피는 [`design-rules.md`](reference/design-rules.md) §6 토스 톤 준수.
   - 낯선 전문용어는 그 슬라이드 하단에 `.gloss` 각주로 짧게 설명한다. 약어는 풀텍스트 병기. ([`design-rules.md`](reference/design-rules.md) §8)
   - `<meta name="deck-*">` 4종(title/date/category/summary)을 채운다(보강 시 date만 갱신).
   - `#counter`·`slides.length`는 실제 장 수에 맞춘다.
7. **자동 분류 (신규만)** — 내용을 보고 최대 4레벨 경로를 정한다. 보강은 기존 경로를 그대로 유지한다.
   - 1레벨: 대분류(기술 / 기획 / 운영 …)
   - 2레벨: 세부(백엔드 / 프론트 …) · 3레벨: 스택/영역(스프링 …) · 4레벨: 선택
   - 필요 없는 레벨은 생략 가능(최소 1레벨).
8. **저장** — 신규: 대상 루트(레포 모드=캐시, 로컬 모드=`<cwd>/docs/decks`) 아래 `<1>/<2>/<3>/<4>/<제목>_<YYYY-MM-DD>.html`(공백→`-`, 특수문자 제거). 보강: 기존 파일 경로에 덮어쓰기.
9. **인덱스 갱신** — `python3 <스킬경로>/scripts/update_index.py <대상 루트>` 실행.
10. **자가 린트** — [`design-rules.md`](reference/design-rules.md) §10 체크리스트를 통과하는지 확인 후 위반 시 수정.
11. **적재 (레포 모드만)** — 캐시에서 `git add -A` → 커밋 → `git push`. **push 성공까지 확인**하고, 실패하면 중단·보고(로컬에만 커밋 남기지 않음). 로컬 모드는 건너뛴다.
12. **옵시디언 Ingest (topic 모드만)** — vault가 설정/응답됐으면 원본→`sources/`, 정리→`wiki/<도메인>/`, `[[링크]]`, `index.md`·`log.md` 갱신. **vault `CLAUDE.md` 스키마 우선.** 미설정·비-topic이면 건너뛴다. (§옵시디언 지식베이스)
13. **오픈** — 생성/보강된 HTML을 `open`으로 실행. 대상(레포/로컬)·경로·장 수·**보강/신규 여부**·**옵시디언 적재 여부**를 한 줄로 보고.

## 절대 규칙

- **외부 라이브러리 금지** — Pretendard 웹폰트 CDN link만 허용. 차트·다이어그램은 순수 CSS/SVG.
- **단일 HTML** — 인라인 CSS/JS, 파일 하나로 완결.
- **디자인 토큰 고정** — `template.html`의 CSS 변수·클래스를 임의로 바꾸지 않는다. 새 표현이 필요하면 기존 컴포넌트를 조합한다.
- **완전 자동** — 분류·구성은 확인 질문 없이 진행. 단, 원문이 비어 컨텍스트가 부족하면 무엇을 다룰지 사용자에게 확인한다.
- **토스 톤** — 모든 카피 해요체·문장형·긍정형. 헤드라인은 완결 주장문(명사 라벨·명사형 종결 금지).
- **용어 각주** — 낯선 용어는 슬라이드 하단에 짧게 설명(슬라이드당 1~3개, 약어는 풀텍스트 병기, 두 줄 이내). 길어지지 않게.
- **타깃 레포는 명시 입력만 인정** — 대상 레포는 `.rp-deck.json` 또는 이번 실행의 사용자 명시 답, 이 둘로만 정한다. ⛔ git remote·대화 맥락·추측으로 레포를 유추·자동 지정 금지. 명시 입력이 없으면 대상을 한 번 물어보고, 답이 없으면 **로컬에만 생성(푸시 없음)** — 생성 자체는 항상 해준다.
- **생성 전 기존 문서 확인** — 대상 루트에 같은 주제가 있으면 보강, 없으면 신규. 애매하면 물어본다. 조용히 덮어쓰지 않는다.
- **작업 전 원격 최신화 필수 (레포 모드)** — 기존 문서 확인·생성 전에 캐시를 `fetch`+`reset --hard origin/<기본브랜치>`로 원격 최신에 맞춘다(stale 캐시로 판단 금지). 각 실행은 push까지 완료.
- **레포 호스트 일반화** — `repo`는 `owner/name`(github.com)과 전체 URL(사내 GitHub Enterprise 등 다른 호스트)을 모두 허용. ⛔ github.com으로 가정하지 말고, 전체 URL이면 그 URL로 clone/fetch·링크한다.
- **옵시디언은 topic 모드만·vault 컨벤션 준수** — 조사 원본은 vault `sources/`에 보존하고 정리는 `wiki/`에. vault `CLAUDE.md` 스키마를 먼저 읽어 그대로 따르고, rp-deck 임의 폴더/형식을 강요하지 않는다. 타깃은 명시 입력만 인정(없으면 한 번 묻고, 답 없으면 스킵). ⛔ 민감정보 기록 금지.

## 확장

`rp-deck`는 여러 입력 모드를 하나의 스킬로 처리한다. 새 입력원(예: URL, PDF)이 필요하면 3단계 입력 수집만 확장하고 이후 파이프라인은 공유한다.
